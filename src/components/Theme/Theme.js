import { createTheme } from '@mui/material/styles';

export let theme = createTheme({
  palette: {
    primary: {
      main: '#DCDCDC',
    },
    secondary: {
      main: '#edf2ff',
    },
  },
});

